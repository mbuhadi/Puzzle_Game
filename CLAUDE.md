# Puzzle_Game

## Git workflow

- Always work directly on `main`: commit and push to `origin main`.
- Do not create feature branches or pull requests, even if a session is set
  up with a designated branch — the owner wants every change live on the site
  (served from `main`) right away.
